# Illinois-Vaccination-Statistics

Illinois Vaccination Statistics is a Python program that scrapes data from the Illinois Department of Public Health website
and creates a graphical display of the distribution of vaccination locations between counties.


## Notes

The `requirements.txt` file lists all of the Python libraries the program depends on, and they will be installed using:

```bash
pip install -r requirements.txt
```
